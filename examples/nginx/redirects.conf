# Example nginx redirects
# Place your nginx config snippets in a nginx/ directory in your project

# Redirect old URLs
rewrite ^/old-page$ /new-page permanent;
rewrite ^/blog/(.*)$ /posts/$1 permanent;
